# Verify Report — flow-where-cross-project-capability-merge

## Status
success

## Change Summary

Doc-only integration of the recovered Phase 2 cross-project search capability into the canonical `flow-where` root spec. The change landed on branch `codex/flow-where-cross-project-capability-merge` at commit `6e21d4d96fbc9d94a0814c0f677cd03cb1f4bb07` (parent: main `920d395`). It touched **3 files / 259 insertions / 2 deletions = 261 net lines** (65% of the 400-line review budget). Zero code changes; baseline preserved at 1513/1513 pytest + mypy clean + the 3 pre-existing ruff errors unchanged.

## 11 ACs Verification

| AC | Description | Result | Evidence |
|----|-------------|--------|----------|
| **AC1** | Delta spec regenerated byte-identical from `27111ed` | **PASSED** | `git hash-object` returns `f546507f4e50d704410a77946529f292cc5b6040` for both `git show 27111ed:openspec/changes/flow-where-cross-project/specs/cross-project-search/spec.md` and `git show HEAD:openspec/changes/flow-where-cross-project-capability-merge/specs/cross-project-search/spec.md`. Strict byte-identity confirmed post-commit. |
| **AC2** | 6 root REQs each with `Source:` line in `flow-where/spec.md` | **PASSED** | Python scan: 6 `REQ-WHERE-*` blocks (`CROSS-PROJECT-SCOPE`, `DEFAULT-TEXT-FORMAT`, `EXPLICIT-FORMAT-FLAG`, `EXIT-CODE-MAPPING`, `ENGRAM-STUB`, `REGEX-OPT-IN`), each containing exactly 1 `**Source:**` line. |
| **AC3** | Each `Source:` path exists on disk | **PASSED** | Single unique path `openspec/changes/flow-where-cross-project-capability-merge/specs/cross-project-search/spec.md` (cited 6 times); `Test-Path` returns True. |
| **AC4** | Each `Source:` REQ-ID exists in cited delta spec | **PASSED** | Delta headings: `REQ-CROSS-PROJECT-SCOPE`, `REQ-DEFAULT-TEXT-FORMAT`, `REQ-EXPLICIT-FORMAT-FLAG`, `REQ-EXIT-CODE-MAPPING`, `REQ-ENGRAM-STUB`, `REQ-REGEX-OPT-IN`. Every root `REQ-WHERE-*` cites exactly one of these IDs. All 6/6 found. |
| **AC5** | `flow-where/spec.md` references `test_cli_where_cross_project.py` (10 tests cited) | **PASSED** | Substring found at §4.b line ~261 with explicit "10 unit tests" annotation. |
| **AC6** | `workspace/spec.md` §6.1 marks reclassification as RESOLVED | **PASSED** | §6.1 contains `[2026-06-30 update — RESOLVED]` paragraph (2 `RESOLVED` literals in §6.1 block at line 292). |
| **AC7** | `workspace/spec.md` §7 row #1 no longer lists `flow-where-cross-project-capability-merge` as pending | **PASSED** | §7 table contains 0 rows referencing the change name (the only matches are: row #5 `workspace-hygiene-r1` with "stash/worktree handling" — legitimate per Batch E #17; and no pending entry for this change). |
| **AC8** | AC9 byte-identical guard still passes (zero code changes) | **PASSED** | `uv run --frozen pytest tests/unit/test_cli_projects.py::test_flow_projects_ls_json_byte_identical_envelope -xvs` → `1 passed in 0.17s`. No code touched (see AC10). |
| **AC9** | Full suite 1513/1513 still passes (zero regressions) | **PASSED** | `uv run --frozen pytest` → `1513 passed, 6 warnings in 68.09s`. Identical baseline to post-workspace-capability-bootstrap main `920d395`. |
| **AC10** | NO modifications to existing flow-where code in `cli.py` (or any other src/tests/pyproject/uv.lock) | **PASSED** | `git diff --name-only 920d395..HEAD -- src/ tests/ pyproject.toml uv.lock` is empty. Four prior archive-capability specs (decision-drift, observability, workspace-hygiene, prompt-registry) and `openspec/changes/v1.1-followups/` are all untouched. |
| **AC11** | Cumulative diff under 400-line review budget | **PASSED** | `git diff --shortstat 920d395..HEAD` = `3 files changed, 259 insertions(+), 2 deletions(-)`. 259 + 2 = 261 net lines = 65% of the 400-line budget. |

## 7 Verify Checks Results

| # | Check | Result | One-line diagnostic |
|---|-------|--------|---------------------|
| 1 | Each `## REQ-WHERE-*` block carries exactly one `Source:` line (6/6) | **PASSED (6/6)** | 6 REQ-WHERE blocks each with exactly 1 `**Source:**` line; 0 duplicates. |
| 2 | Every `Source:` path exists on disk | **PASSED (1 unique, exists)** | `openspec/changes/flow-where-cross-project-capability-merge/specs/cross-project-search/spec.md` (cited 6×) — file exists, 155 lines. |
| 3 | Every `Source:` REQ-ID exists in cited delta spec | **PASSED (6/6)** | Delta defines exactly the 6 IDs cited by the 6 root blocks. |
| 4 | `flow-where/spec.md` cites `test_cli_where_cross_project.py` | **PASSED** | Substring found in §4.b test-pointer paragraph with "10 unit tests" annotation. |
| 5 | `workspace/spec.md` §6.1 marks reclassification as RESOLVED | **PASSED** | `RESOLVED` literal appears 2× in §6.1 (line 292 `[2026-06-30 update — RESOLVED]` + paragraph). |
| 6 | `workspace/spec.md` §7 row #1 no longer lists `flow-where-cross-project-capability-merge` as pending | **PASSED** | §7 table contains 0 rows referencing this change name. |
| 7 | `flow-where/spec.md` §8 Drift Detection footer present | **PASSED** | `^## 8\. Drift Detection` matches exactly once (line 336). |

## Post-Commit Hash Check (T-8 contract)

The byte-identical recovery contract was independently verified post-commit:

```
Source SHA from 27111ed:   f546507f4e50d704410a77946529f292cc5b6040
Committed SHA at HEAD:     f546507f4e50d704410a77946529f292cc5b6040
Expected (user-locked):    f546507f4e50d704410a77946529f292cc5b6040
```

Both hashes match the expected SHA exactly. The recovered delta survived the commit byte-identical — no mutation occurred during staging or commit.

Verification method:

```powershell
$committedHash = (git show HEAD:openspec/changes/flow-where-cross-project-capability-merge/specs/cross-project-search/spec.md | git hash-object --stdin).Trim()
if ($committedHash -eq "f546507f4e50d704410a77946529f292cc5b6040") { Write-Output "PASSED" } else { exit 1 }
```

## Baseline Preservation Gates

| Gate | Command | Result | Notes |
|------|---------|--------|-------|
| pytest | `uv run --frozen pytest` | **1513 passed, 6 warnings in 68.09s** | Identical to baseline at main HEAD `920d395`. Zero regressions. |
| AC9 byte-identical guard | `uv run --frozen pytest tests/unit/test_cli_projects.py::test_flow_projects_ls_json_byte_identical_envelope -xvs` | **1 passed in 0.17s** | The Phase 1 `flow projects ls --json` envelope byte-identity check remains green. |
| mypy | `uv run --frozen mypy src/` | **Success: no issues found in 32 source files** | Strict type check clean. |
| ruff | `uv run --frozen ruff check .` | **Found 3 errors (pre-existing OOS)** | Identical to Phase 4 baseline: `cli.py:682 RET504`, `test_cli_where_cross_project.py:33 UP035`, `test_cli_where_cross_project.py:295 W292`. Doc-only change did not introduce or fix lint errors. |

## Commit Hygiene

| Field | Value |
|-------|-------|
| Commit SHA | `6e21d4d96fbc9d94a0814c0f677cd03cb1f4bb07` |
| Branch | `codex/flow-where-cross-project-capability-merge` (local only, NOT pushed) |
| Parent | `920d395` (main HEAD post-workspace-capability-bootstrap) |
| Subject (line 1) | `docs(specs): integrate Phase 2 cross-project into flow-where root` |
| Conventional format | Yes (`docs(specs):` prefix; present tense) |
| Files in commit | 3 |
| Insertions | 259 |
| Deletions | 2 |
| Co-Authored-By | ABSENT |
| AI attribution | ABSENT |
| Body shape | Bullet list of 6 deliverables + cumulative-diff summary; clean |

## Special Cases

### workspace §7 "stash" mention — context interpretation

The canonical spec at `openspec/specs/workspace/spec.md` §7 contains, at row #5 of the Future Changes table:

```
| 5 | `workspace-hygiene-r1` (deferred) | R1 dirty-git remediation: stash/worktree handling,
  interactive prompts, status integration. Explicitly OUT of Phase 4. | Low |
  Future change if requested |
```

This is **legitimate carry-over content**, NOT a violation:

- It is part of the `workspace-capability-bootstrap` archive-record surface (engram #498 + #506). The phrase describes the **deferred scope of a future change** (`workspace-hygiene-r1` is OUT of Phase 4), not the implementation of any prohibited behavior.
- It is consistent with the user's "Constraints have context" principle (engram #496): the word "stash/worktree" is acceptable when it qualifies *what a future R1 change might handle*, not when it appears as instruction to act on it.
- The future `workspace-hygiene-r1` change, if/when implemented, would re-evaluate this constraint at its own design phase.
- This verify run does NOT flag this row as a violation (per Batch E constraint #17).
- It does not block this PR; the row will be retired only when the R1 change actually ships (or is explicitly abandoned in a separate change).

## Risks / Warnings / Critical

Carried from design #508 §7 (follow-up tech debt — NOT blockers for this PR):

- **T1 (WARN)** — `workspace/spec.md:241` §4.2 versioning row still reads "No change to workspace root spec; `flow-where/spec.md` gains `REQ-V1.0.5..V1.0.X`". The actual landed REQs are `REQ-WHERE-CROSS-PROJECT-SCOPE` … `REQ-WHERE-REGEX-OPT-IN` (distinct namespace). Tracked as a future `workspace-spec-cross-impact-cleanup` follow-up. NOT in scope here.
- **T2 (WARN)** — `workspace/spec.md:16` archive-status carry-forwards list still mentions `flow-where-cross-project-capability-merge`. Prose is stale; the change is now resolved. Future cleanup change.
- **T3 (SUGGEST)** — Automated drift detection (parse `Source:` lines, validate path + REQ-ID + diff root REQ prose against delta). Deferred to a future `spec-drift-detector` change. Not blocking.
- **T4 (SUGGEST)** — Cross-link validation across ALL capability specs (extend Checks 1-3 to `decision-drift`, `observability`, `prompt-registry`). Future `capability-spec-linter` change.
- **T5 (SUGGEST)** — Polish/improvements to the recovered Phase 2 delta spec (typo fixes, formatting). Future SEPARATE `cross-project-search-content-polish` change.

No CRITICAL findings. No contamination. No "stash/worktree" instruction violations.

## Verdict

**PASS** — green for `sdd-archive`.

All 11 ACs PASSED, all 7 verify checks PASSED, baseline preserved at 1513/1513 pytest + mypy clean + AC9 byte-identical guard green + ruff 3 pre-existing OOS. The byte-identical hash contract (`f546507f4e50d704410a77946529f292cc5b6040`) survived commit. Zero code changes. The single commit `6e21d4d` on local branch `codex/flow-where-cross-project-capability-merge` is ready for archive and user merge decision.

## Next Steps

1. `sdd-archive flow-where-cross-project-capability-merge` — move `openspec/changes/flow-where-cross-project-capability-merge/` to `openspec/changes/archive/2026-06-30-flow-where-cross-project-capability-merge/`. No spec merge step needed (the change already landed directly in the canonical specs at apply time, per the additive append pattern).
2. User opens the PR (single PR, 1 commit, no chained) and performs the merge to main. No push required from verify/apply.
3. Post-merge housekeeping (future changes, NOT in this PR's scope): items T1/T2/T3/T4/T5 from the Risks section.
